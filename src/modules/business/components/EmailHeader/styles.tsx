import styled, { StyledComponent } from 'styled-components';

export const StyledEmailHeader: StyledComponent<any, any> = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;
